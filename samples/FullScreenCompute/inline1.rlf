
Dispatch {
	Shader = ComputeShader {
		ShaderPath = "shader.hlsl";
		EntryPoint = "CSMain";
	};
	ThreadPerPixel = true;
	Bind OutTexture = @BackBuffer;
} perPixelDispatch

Passes {
	perPixelDispatch
}
