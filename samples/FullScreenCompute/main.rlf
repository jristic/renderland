
ComputeShader {
	ShaderPath = "shader.hlsl";
	EntryPoint = "CSMain";
} testShader

Dispatch {
	Shader = testShader;
	ThreadPerPixel = true;
	Bind OutTexture = @BackBuffer;
} perPixelDispatch

Passes {
	perPixelDispatch
}
